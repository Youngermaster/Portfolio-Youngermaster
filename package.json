{
  "name": "portfolio_nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "buildOut": "rm -rf out && next build && next export",
    "start": "next start",
    "githubExport": "npm run build && cd out/ && cp -r * ~/GitHub/Youngermaster/youngermaster.github.io"
  },
  "dependencies": {
    "next": "12.0.7",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-icons": "^4.3.1",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7"
  }
}